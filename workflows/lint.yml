name: Bash Linting Pipeline
on: [push]
jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Shellcheck Engine
        run: sudo apt-get update && sudo apt-get install -y shellcheck
      - name: Run Quality Verification Check
        run: shellcheck *.sh
